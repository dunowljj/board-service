spring:
  application:
    name: board-service

  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    open-in-view: false

observability:
  query:
    value-allowlist:
      - page
      - size
      - sort
